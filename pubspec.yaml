name: namer_app
description: A new Flutter project.

publish_to: 'none' # Remove this line if you wish to publish to pub.dev

version: 0.0.1+1

environment:
  sdk: ^3.6.0

dependencies:
  json_annotation: ^4.9.0
  flutter:
    sdk: flutter

  english_words: ^4.0.0
  http: ^1.3.0
  path_provider: ^2.0.12
  flutter_riverpod: ^2.0.0
  shared_preferences: ^2.0.17

dev_dependencies:
  flutter_test:
    sdk: flutter

  build_runner: ^2.4.15
  json_serializable: ^6.9.4

  flutter_lints: ^5.0.0

flutter:
  uses-material-design: true
  assets:
    - assets/response.json
    - assets/recentPosts.json
    - assets/postDataBase.json
    - assets/cat.jpg
    - assets/userDataBase.json